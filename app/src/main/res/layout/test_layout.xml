<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/teal_200"
    android:orientation="vertical" >

   <RadioGroup
        android:id="@+id/radioGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:weightSum="1" >

        <RadioButton
            android:id="@+id/PresentradioButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="40dp"
            android:focusable="false"
			android:focusableInTouchMode="false"
            android:text="PRESENT"
            android:textSize="40sp" android:layout_marginTop="70dp" />

        <RadioButton
            android:id="@+id/AbsentradioButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"   android:layout_marginLeft="40dp"
            android:focusable="false"
			android:focusableInTouchMode="false"
            android:text="ABSENT"
            android:textSize="40sp" android:layout_marginTop="70dp"/>
.>      <Button
            android:id="@+id/attendanceSubmitButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/button" android:textSize="30sp"
            android:text="Submit" android:layout_marginLeft="130dp"
            android:layout_marginTop="150dp" android:textColor="@color/black"/>
    </RadioGroup>
</LinearLayout>